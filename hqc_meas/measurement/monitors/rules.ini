[rule0]
    new_entry_suffix = progress
    new_entry_formatting = {index} / {points_number}
    hide_entries = True
    suffixes = "['index', 'point_number']"
    class = FormatRule
    name = Loop progress
